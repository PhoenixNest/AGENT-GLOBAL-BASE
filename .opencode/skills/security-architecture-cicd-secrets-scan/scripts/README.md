# Scripts

Bundled scripts, configurations, and templates for this skill.

## Contents

| File                  | Purpose                                            |
| --------------------- | -------------------------------------------------- |
| `pre-commit-hook.yml` | Pre-commit hook configuration for secret detection |
| `secrets-config.yml`  | CI secrets scan configuration                      |
| `scan-config.sh`      | Git history scan for leaked secrets                |

## Usage

Reference scripts from this directory in the skill's execution guidance.
