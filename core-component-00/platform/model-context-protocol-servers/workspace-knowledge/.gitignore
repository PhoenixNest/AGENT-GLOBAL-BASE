# Runtime-generated artifacts — never commit these
embedding/

# Local evaluation harness — test scripts and generated outputs stay local,
# except permanent regression tests explicitly excepted below
tests/*
!tests/test_upsert_delete_ordering_fix.py
!tests/test_search_tier_degradation.py

# Python environment
.venv/
__pycache__/
*.py[cod]

# RAG sync state (machine-local index metadata, not committed)
rag-system/rag-sync-state.json
