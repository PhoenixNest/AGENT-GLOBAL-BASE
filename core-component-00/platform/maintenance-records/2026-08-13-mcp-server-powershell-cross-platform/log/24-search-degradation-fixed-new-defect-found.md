# Log Entry 24 — Execution — 2026-09-01

| Field            | Detail                                                                                                                                                                                                              |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Part of**      | `core-component-00/platform/maintenance-records/2026-08-13-mcp-server-powershell-cross-platform/maintenance-record.md`, pipeline stage 3 — Execution (`core-component-00/platform/maintenance-records/pipeline.md`) |
| **Trigger**      | `log/23`'s Approval — execute both remediations for Item #9.                                                                                                                                                        |
| **State before** | `agent-memory` search-degraded (`effective_path: "unavailable"`); `workspace-knowledge` on BM25 fallback (`FAISS init failed` on the broken `AGENTS.md` symlinks).                                                  |

**Actions taken:**

1. Ran `_shared/provision_model.py sentence-transformers/all-MiniLM-L6-v2` (via `agent-memory`'s venv) — succeeded, 87.3 MB, 384-dim.
2. Ran `_shared/provision_model.py sentence-transformers/all-mpnet-base-v2` (via `workspace-knowledge`'s venv) — succeeded, 418.4 MB, 768-dim. Confirmed `_shared/models/` now holds both slug directories.
3. Re-derived the 8 broken symlinks independently (`git ls-files -s | grep AGENTS.md`, blob-hash diff), confirming `log/23`'s correction. For each, removed and recreated with `os.symlink("CLAUDE.md", path)`, verifying via `os.readlink()` that the new target is exactly `"CLAUDE.md"` (9 bytes, no trailing newline):
   - `core-component-00/framework/00-agent-systems-governance-framework/AGENTS.md`
   - `core-component-00/framework/01-prompt-engineering/AGENTS.md`
   - `core-component-00/framework/02-context-engineering/AGENTS.md`
   - `core-component-00/framework/03-harness-engineering/AGENTS.md`
   - `core-component-00/framework/04-retrieval-augmented-generation/AGENTS.md`
   - `core-component-00/framework/05-multi-agent-engineering/AGENTS.md`
   - `core-component-00/platform/maintenance-records/AGENTS.md`
   - `core-component-00/platform/model-context-protocol-servers/AGENTS.md`
   - Confirmed via `git status` that exactly these 8 files show as modified — none of the other ~20 correct `AGENTS.md` symlinks were touched.
4. Called `mcp__agent-memory__search_memory` twice to force the in-process fallback to actually load (it is lazy, only triggered by a real search call) rather than trusting a passive `health_check` read.
5. Called `mcp__workspace-knowledge__rebuild_index` to force `workspace-knowledge` to re-run its FAISS init against the now-fixed symlinks — its cached degraded state from server startup would not otherwise self-heal.

**Verification:**

| Check performed                                                             | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `agent-memory` `health_check`, before `search_memory` calls                 | `search_capability.effective_path: "unavailable"` (unchanged from `log/22`)                                                                                                                                                                                                                                                                                                                                                                                             |
| `mcp__agent-memory__search_memory` call #1                                  | `degraded: true`, `reason: "embedding unavailable (embedder-service: unavailable; in-process fallback: loading)"` — fallback triggered, still warming up                                                                                                                                                                                                                                                                                                                |
| `mcp__agent-memory__search_memory` call #2                                  | `degraded: false`, `reason: null`, `tier: 1` — fallback ready                                                                                                                                                                                                                                                                                                                                                                                                           |
| `agent-memory` `health_check`, after                                        | `search_capability: {"embedder_service_state": "unavailable", "in_process_fallback_state": "ready", "effective_path": "in-process-fallback"}` — **search capability confirmed fixed**, though the standalone shared `embedder-service` process itself never came up for this server (this session's `agent-memory` process used its in-process fallback directly rather than the shared service; not investigated further as out of Item #9's scope)                    |
| `mcp__workspace-knowledge__rebuild_index`                                   | `{"status": "rebuilt", "tier": "bm25", "degradation_reason": "FAISS init failed: ... could not open .../workspace-knowledge/embedding/faiss.index for writing: No such file or directory — using BM25"}` — **the original `AGENTS.md` `FileNotFoundError` is gone**, confirming the symlink fix is effective and the init pipeline advanced further than before, but a **new, previously-masked failure** now blocks it                                                 |
| Read `workspace-knowledge/server.py:389-436` (`_build_or_load_faiss_index`) | Confirmed root cause of the new failure: `faiss.write_index(index, str(index_file))` (line 432) and `state_file.write_text(...)` (line 433) are called with no prior `self._INDEX_DIR.mkdir(parents=True, exist_ok=True)` — a genuine code defect, not a config issue. `workspace-knowledge/embedding/` is gitignored/runtime-only (never committed) and does not exist on this machine, since this is the first time the full FAISS-build path has ever executed here. |

| Field                     | Detail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Outcome**               | Both `log/22`/`log/23`-approved remediations are executed and confirmed effective for their stated root causes. **`agent-memory`'s search degradation is fully resolved** — verified live, not just inferred. **`workspace-knowledge`'s symlink defect is fully resolved** — the specific `FileNotFoundError` `log/22` root-caused no longer occurs. However, `workspace-knowledge` **remains on BM25** because fixing the symlink exposed a second, previously-masked, distinct code defect: `_build_or_load_faiss_index` never creates its output directory before writing to it, so it fails outright on any machine where `embedding/` hasn't already been created by some other means. Per `pipeline.md`'s guidance not to keep executing past a newly-found problem without Approval, this was **not fixed** — it is reported as a new Open Follow-Up Item #10, not applied. |
| **Handoff to next stage** | Item #9 **closes** — both of its originally-diagnosed root causes are fixed and verified. New **Item #10 opens** (not yet approved): `workspace-knowledge/server.py`'s FAISS-index write path needs a `mkdir(parents=True, exist_ok=True)` guard before `faiss.write_index`/`state_file.write_text`; the immediate on-this-machine workaround (creating the missing `embedding/` directory by hand) was also not applied, pending Approval on whether to do the workaround, the code fix, or both.                                                                                                                                                                                                                                                                                                                                                                                 |
