# Log Entry 30 — Execution — 2026-09-02

| Field            | Detail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Part of**      | `core-component-00/platform/maintenance-records/2026-08-13-mcp-server-powershell-cross-platform/maintenance-record.md`, pipeline stage 3 — Execution (`core-component-00/platform/maintenance-records/pipeline.md`)                                                                                                                                                                                                                                                                      |
| **Trigger**      | CEO-approved N1 fix for Item #11, opened by `log/29-fresh-clone-dry-run-first-session-gap-found.md`. Directed to work from a fresh isolated git worktree branched from `core00/dev/engineering` (`agent/infrastructure/fresh-clone-mcp-json-self-heal`), scoped to `.claude/hooks/` only.                                                                                                                                                                                                |
| **State before** | Confirmed this worktree genuinely lacked `.mcp.json` and both servers' `.venv/` before starting (same fresh-clone proxy `log/29` used). `mcp-config-platform-check.py` and `mcp-venv-bootstrap.py` were unmodified since `log/20`/`log/29` — the gap `log/29` found (single-`SessionStart` chain leaves `.mcp.json` pointing at a nonexistent interpreter path for a server whose venv `mcp-venv-bootstrap.py` had to create, self-healing only on a session restart) was still present. |
| **Severity**     | P1 (confirmed defect with a workaround) → closed by this entry.                                                                                                                                                                                                                                                                                                                                                                                                                          |

**Actions taken:**

1. Read `.claude/hooks/mcp-config-platform-check.py` and `.claude/hooks/mcp-venv-bootstrap.py` in
   full, plus `.claude/settings.json`'s `SessionStart` registration (confirmed: platform-check
   registered first, venv-bootstrap second, both `uv run`), per `log/29`'s own invocation
   contract.
2. Implemented `log/29`'s candidate fix (a): `mcp-venv-bootstrap.py` now re-invokes
   `mcp-config-platform-check.py` a second time — `_rerun_platform_check()`
   (`.claude/hooks/mcp-venv-bootstrap.py:130-160`), called from `main()` only when `synced` is
   non-empty (`.claude/hooks/mcp-venv-bootstrap.py:249`) — via `subprocess.run(["uv", "run",
str(PLATFORM_CHECK_SCRIPT)], input=raw_stdin, ...)`, the same invocation shape `SessionStart`
   itself uses, feeding it this session's own stdin payload so the resulting
   `hook-invocations.jsonl` record correlates by `session_id`. Its stdout is captured, not
   forwarded, so this hook's own stdout stays exactly one JSON object for `SessionStart` to
   parse. Gated strictly behind "at least one server was actually synced" — the fast/no-op path
   for the overwhelming majority of ordinary sessions (both venvs already exist) is untouched, and
   `mcp-config-platform-check.py` itself was not modified in any way that changes its own
   fast/fail-open contract (docstring-only addition pointing at this fix).
3. Reworded the hook's own user-facing follow-up message
   (`.claude/hooks/mcp-venv-bootstrap.py:262-271`): when the re-check succeeds, it now correctly
   says `/mcp` reconnect will work (true again, since `.mcp.json` is corrected by that point);
   when the re-check itself fails open, it now says a session restart is needed instead of
   repeating the old unconditional "reconnect" guidance `log/29` found to be misleading in the
   broken case.
4. Wrote a lightweight regression test, `.claude/hooks/test_mcp_session_start_self_heal.py`
   (no prior test file existed for either hook — checked first, per instructions) — an isolated
   fake repo (own `git init`, copied hook scripts, a stub `uv` that fakes `sync` instantly and
   forwards `run <script>` to the real interpreter, so the real hook code under test executes
   unmodified without a real network/GPU dependency install). Two tests: the fresh-clone sequence
   ends the same `SessionStart` pass with a corrected `.mcp.json`, and an ordinary session (both
   venvs + a correct `.mcp.json` already present) stays a fast no-op for both hooks.
5. Verified the fix against the **real** hook chain, not just the fake-repo test — simulated the
   exact fresh-clone condition in this worktree (confirmed `.mcp.json` and both `.venv/` absent),
   then ran both `SessionStart` hooks in registered order exactly as Claude Code would: `uv run
.claude/hooks/mcp-config-platform-check.py` then `uv run .claude/hooks/mcp-venv-bootstrap.py`,
   each fed `{"session_id": "verify-session-1"}` on stdin, `CLAUDE_PROJECT_DIR` set to the
   worktree root — the same contract `log/29` used. This performed a real `uv sync` for both
   servers (not stubbed) — `~/.cache/uv` was already warm from prior sessions on this machine, so
   both completed in well under the hooks' own timeouts.
6. Re-ran both hooks a third time with a fresh `session_id` (both venvs and a correct `.mcp.json`
   now already in place) to confirm the fix does not regress the ordinary-session fast-no-op
   contract for either hook.

**Verification:**

| Check performed                                                                    | Result                                                                                                                                                                                                                                                                                                                       |
| ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.claude/hooks/test_mcp_session_start_self_heal.py` (2 tests)                      | Both pass — fresh-clone sequence corrects `.mcp.json` within one pass; ordinary session stays a fast no-op for both hooks.                                                                                                                                                                                                   |
| Real fresh-clone simulation, hook 1 (`mcp-config-platform-check.py`)               | `.mcp.json` bootstrapped from `.mcp.json.example`'s Windows default (`.venv/Scripts/python.exe`, both servers), `hook-invocations.jsonl` recorded `decision: "bootstrapped"`, `unresolved: [{workspace-knowledge, neither_os_path_exists}, {agent-memory, neither_os_path_exists}]` — reproduces `log/29`'s finding exactly. |
| Real fresh-clone simulation, hook 2 (`mcp-venv-bootstrap.py`)                      | Both `uv sync` calls succeeded; stdout's own message now includes `"- .mcp.json interpreter path(s) re-verified for the newly-created venv(s)"` and the original "reconnect" guidance (now accurate again).                                                                                                                  |
| `.mcp.json` content **after hook 2 completes, same pass, no further action**       | **Corrected** — both servers now read `.venv/bin/python` (POSIX), not `.venv/Scripts/python.exe`. This is the fix: `log/29` found this file still wrong at this exact point; it is now right.                                                                                                                                |
| `hook-invocations.jsonl`, full sequence for `session_id: "verify-session-1"`       | Three records in order: platform-check `bootstrapped` → platform-check `corrected` (the re-check, same session_id) → venv-bootstrap `synced` with `post_sync_recheck: {"ok": true, "returncode": 0}`.                                                                                                                        |
| `test -f .../.venv/bin/python` for both servers                                    | Present — real interpreters, `sys.executable` resolves correctly when invoked directly (not a full server foreground launch, to avoid GPU/port contention with other worktrees' running processes on this shared machine).                                                                                                   |
| Second pass, fresh `session_id`, both venvs + correct `.mcp.json` already present  | Both hooks: no stdout emitted, `hook-invocations.jsonl` records `decision: "no_change"` / `"no_action_needed"` — fast/fail-open contract for the ordinary-session case confirmed unchanged.                                                                                                                                  |
| `pytest .claude/hooks/test_context_budget_alert.py` (pre-existing, unrelated hook) | 3 pre-existing failures, unrelated to this change (that hook was not touched) — not a regression introduced here.                                                                                                                                                                                                            |

| Field                     | Detail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Outcome**               | Item #11 fixed: a genuinely fresh clone's single, fully-automated `SessionStart` pass now ends with a correct `.mcp.json` for this OS, verified against the real hook chain (not only the fake-repo regression test) in an isolated worktree that reproduced `log/29`'s exact starting condition. `mcp-config-platform-check.py`'s own fast/fail-open contract is unchanged (docstring-only edit); `mcp-venv-bootstrap.py` gains one bounded re-invocation, only on the already-rare path where `uv sync` actually ran. Not a live CEO `/mcp reconnect` against the primary workspace — this entry's verification is self-verification against the real hook chain in an isolated worktree, the same class of evidence `log/29` itself used to open this item. Work stayed on `agent/infrastructure/fresh-clone-mcp-json-self-heal`, not merged to `master` or `core00/dev/engineering`, per instructions. |
| **Handoff to next stage** | Item #11 closes below. If the CEO wants a live end-to-end confirmation on an actual fresh clone (matching the stage-4 independent-review pattern used for `log/07`, `log/13`, `log/17`, `log/21`, `log/27` elsewhere in this topic), that remains available as a follow-up but is not required to close this specific gap, since the fix was verified against the real mechanism `log/29` diagnosed, not merely asserted. Item #3 (Linux/macOS DR backup registration, unrelated) remains the topic's only other open item.                                                                                                                                                                                                                                                                                                                                                                                |
