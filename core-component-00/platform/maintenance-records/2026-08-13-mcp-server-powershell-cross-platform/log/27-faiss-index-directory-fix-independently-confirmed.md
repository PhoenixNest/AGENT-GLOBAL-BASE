# Log Entry 27 — Verification — 2026-09-01

| Field            | Detail                                                                                                                                                                                                                                                                                         |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Part of**      | `core-component-00/platform/maintenance-records/2026-08-13-mcp-server-powershell-cross-platform/maintenance-record.md`, pipeline stage 4 — Verification (`core-component-00/platform/maintenance-records/pipeline.md`)                                                                         |
| **Trigger**      | `log/26`'s Execution — the `mkdir` guard fix was implemented and confirmed in isolation, but per `pipeline.md`'s independent-review gate, a fix to a shared production resource needs live end-to-end confirmation from someone other than the executor, not just an isolated unit-level test. |
| **State before** | Fix implemented in `server.py`, but the three live `workspace-knowledge` processes on this machine still held the pre-fix code in memory; no live process had yet run the patched `_build_or_load_faiss_index` against real embeddings.                                                        |

**Actions taken:**

1. CEO ran `/mcp` in their own session; both `agent-memory` and `workspace-knowledge` reported "Reconnected" — restarting `workspace-knowledge`'s process onto the patched `server.py`.
2. Called `workspace-knowledge`'s `health_check` immediately after reconnect — caught the expected transient startup state (`reachable: false`, `search_tier: "bm25"`, `degradation_reason: "... model not ready — deferred to post-FAISS init"`), not a new failure.
3. Called `workspace-knowledge`'s `rebuild_index` to force the full FAISS-build path to run to completion and get a deterministic result rather than a possibly-still-warming-up passive read. It ran for 4m42s (full corpus re-embed, ~3957 chunks) before completing.
4. Called `health_check` again after `rebuild_index` returned, to independently corroborate the tool's own reported result rather than trusting it alone.

**Verification:**

| Check performed                             | Result                                                                                                                                                                                             |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CEO's live `/mcp` reconnect                 | Pass — "Reconnected to agent-memory." / "Reconnected to workspace-knowledge."                                                                                                                      |
| `workspace-knowledge` `rebuild_index`       | `{"status": "rebuilt", "tier": "hybrid_qdrant", "degradation_reason": null, "qdrant_ready": true, "result_quality": "hybrid_qdrant"}` — full FAISS/Qdrant hybrid build succeeded, no BM25 fallback |
| `workspace-knowledge` `health_check`, after | `document_knowledge_base: {"reachable": true, "point_count": 3957, "search_tier": "hybrid_qdrant", "degradation_reason": null}` — fully healthy, no degradation                                    |

| Field                     | Detail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Outcome**               | Item #10 closes. The CEO's live reconnect plus a full, independently-called `rebuild_index`/`health_check` round-trip confirm the `mkdir` guard fix resolves the FAISS-write crash end-to-end, on real data, with real embeddings — not just in the isolated unit-level test from `log/26`. `workspace-knowledge` is now fully healthy (`hybrid_qdrant`, no degradation). This also closes out the topic's full chain from Item #8 through Item #10: venv bootstrap, model provisioning, symlink repair, and the FAISS-directory fix are all verified working together on this machine. |
| **Handoff to next stage** | Item #10 closes (Close). No further open items surfaced by this topic's chain of investigation; Item #3 (Linux/macOS DR scheduling) remains open and unrelated, deferred at P3.                                                                                                                                                                                                                                                                                                                                                                                                         |
