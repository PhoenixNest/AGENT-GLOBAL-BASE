# Log Entry 22 — Investigation — 2026-09-01

| Field            | Detail                                                                                                                                                                                                                                                                                            |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Part of**      | `core-component-00/platform/maintenance-records/2026-08-13-mcp-server-powershell-cross-platform/maintenance-record.md`, pipeline stage 1 — Investigation (`core-component-00/platform/maintenance-records/pipeline.md`)                                                                           |
| **Trigger**      | Item #9, opened during Item #8's Stage-4 verification: `health_check` calls on both servers showed them reachable but search-degraded — `agent-memory`'s `search_capability.effective_path: "unavailable"`, `workspace-knowledge`'s `degradation_reason: "FAISS init failed: ... AGENTS.md ..."`. |
| **State before** | Both degradations observed only via `health_check` output; root cause unknown for either.                                                                                                                                                                                                         |

**Actions taken:**

1. Confirmed no GPU/CUDA issue: `nvidia-smi` shows an RTX 4060 present, and `agent-memory`'s venv reports `torch.cuda.is_available() == True`.
2. Checked for a running `embedder-service` process and its `run/embedder-service.{lock,pid}` files — none found; the `run/` directory exists but is empty.
3. Launched `_shared/embedder-service/server.py` directly in the foreground (agent-memory's venv, 15s timeout) to capture its real startup error rather than inferring from `health_check` alone. Cleaned up the empty `run/` dir contents afterward (there was nothing to clean — it never wrote a lock/pid file because it crashed before doing so).
4. Confirmed `core-component-00/platform/model-context-protocol-servers/_shared/models/` does not exist at all on this machine (`ls`: No such file or directory) — the shared, gitignored, machine-local embedding-model cache described in `.claude/rules/mcp-governance.md` has never been provisioned here.
5. Read `workspace-knowledge/server.py`'s FAISS-shadow-init code path (`_init_faiss_background` → `_build_or_load_faiss_index`) to find exactly where the reported path is touched.
6. Reproduced the FAISS crash live and directly, by importing `server.py` in-process (workspace-knowledge's venv, correct `WORKSPACE_ROOT`) and calling `_build_or_load_faiss_index` directly with a full traceback, rather than trusting the stringified `degradation_reason` alone.
7. Once the crashing line (`md_file.stat().st_mtime`) was identified, checked the specific file directly: `ls -la`, `os.readlink()`, `Path.exists()`/`Path.stat()` in Python, and the underlying git blob content (`git cat-file -p`) for `core-component-00/framework/04-retrieval-augmented-generation/AGENTS.md`.
8. Swept the whole repo for every `AGENTS.md` symlink (`git ls-files -s | grep AGENTS.md`) and diffed their blob hashes to find the scope of the defect, and checked `git log` on the affected path to identify the introducing commit.

**Verification:**

| Check performed                                                                                                                                     | Result                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `nvidia-smi` / `torch.cuda.is_available()` in agent-memory's venv                                                                                   | GPU present, CUDA available — not the cause                                                                                                                                                                                                                                                                                                                                                                                      |
| Foreground launch of `embedder-service/server.py`                                                                                                   | Crashes immediately: `RuntimeError: no models could be loaded from .../_shared/models — provision at least one via _shared/provision_model.py before starting embedder-service`. Both `sentence-transformers--all-MiniLM-L6-v2` and `--all-mpnet-base-v2` reported `SKIP ... not provisioned`.                                                                                                                                   |
| `ls .../_shared/models/`                                                                                                                            | Directory does not exist on this machine                                                                                                                                                                                                                                                                                                                                                                                         |
| Direct in-process reproduction of `_build_or_load_faiss_index` with a full traceback                                                                | `FileNotFoundError` at `server.py:401`, `md_file.stat().st_mtime`, for `.../core-component-00/framework/04-retrieval-augmented-generation/AGENTS.md`                                                                                                                                                                                                                                                                             |
| `os.readlink()` on that path                                                                                                                        | Raw target is literally `'CLAUDE.md\n'` — a trailing newline baked into the symlink target, not `'CLAUDE.md'`                                                                                                                                                                                                                                                                                                                    |
| `git cat-file -p` on that symlink's blob (`ceb2b988...`) vs. every correctly-working `AGENTS.md` symlink elsewhere in the repo (blob `681311eb...`) | Byte-for-byte confirmed: the broken blob is `434c 4155 4445 2e6d 64 0a` (`"CLAUDE.md\n"`, 10 bytes); the correct blob used by every other `AGENTS.md` symlink in the repo is `434c 4155 4445 2e6d 64` (`"CLAUDE.md"`, 9 bytes, no trailing newline)                                                                                                                                                                              |
| `git ls-files -s \| grep AGENTS.md` sweep                                                                                                           | Exactly 9 symlinks carry the broken blob — all and only the `AGENTS.md` files under `core-component-00/framework/*/`, `core-component-00/platform/maintenance-records/`, and `core-component-00/platform/model-context-protocol-servers/`; every other `AGENTS.md` in the repo (`company/`, `studio/`, `telescope/`, `academic-neural-unit-00/`, `core-component-00/`, `core-component-00/crew/`, etc.) carries the correct blob |
| `git log -- .../framework/04-retrieval-augmented-generation/AGENTS.md`                                                                              | Only two commits touch it: `c8efa649` ("reorganize into framework/ and platform/") and `905e14b7` ("re-run Prettier on reorg-touched Markdown files") — exactly the reorg that created the `framework/`/`platform/` directory structure these 9 broken symlinks live under                                                                                                                                                       |

| Field                     | Detail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Outcome**               | Both degradations are fully root-caused, and both are confirmed **pre-existing and unrelated to Item #8's venv-bootstrap fix** — surfaced only because this is the first time either server has run its full init pipeline on this fresh machine. **(1) `agent-memory` embedder-service:** never provisioned — `_shared/models/` doesn't exist on this machine at all, so `embedder-service` crashes on every launch attempt with `RuntimeError`, and `agent-memory`'s in-process fallback simply hasn't been triggered yet (it's lazy, only starts on a real `search_memory` call). Same class of gap as Item #8 (first-time machine setup never done) — the fix is a one-time `_shared/provision_model.py <hf-model-id>` run for both required models, per the documented convention in `.claude/rules/mcp-governance.md`. **(2) `workspace-knowledge` FAISS shadow-index:** a genuine repo defect, not a machine-setup gap. Exactly 9 `AGENTS.md -> CLAUDE.md` symlinks (all under the paths created by the `c8efa649` reorg) were committed with a corrupted target string containing a trailing newline (`"CLAUDE.md\n"` instead of `"CLAUDE.md"`), making them broken symlinks from any tool that resolves them programmatically (`ls`/`cat` mask this visually; `Path.stat()`/`Path.exists()` do not). `workspace-knowledge`'s file-mtime walk hits one of these nine and crashes, which — because it degrades to BM25 rather than raising — has silently masked this defect until a `health_check` was actually inspected today. This does not affect connectivity, `agent-memory`, or the venv-bootstrap work; it is a distinct, narrowly-scoped bug in 9 specific files, fully enumerated. |
| **Handoff to next stage** | Two independent remediation recommendations for Approval, not yet executed: (1) provision the two required embedding models into `_shared/models/` on this machine via `_shared/provision_model.py`, unblocking `embedder-service` for both consumers. (2) Recreate the 9 broken `AGENTS.md` symlinks with the correct no-trailing-newline target (matching the other 20 correct ones already in the repo) — a trivial, mechanical, low-risk fix; the exact 9 paths and both blob hashes are enumerated in this entry's Verification table above. Item #9 stays open pending Approval on either or both.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
