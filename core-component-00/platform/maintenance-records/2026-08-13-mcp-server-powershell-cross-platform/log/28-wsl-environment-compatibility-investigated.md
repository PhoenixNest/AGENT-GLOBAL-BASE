# Log Entry 28 — Investigation — 2026-09-01

| Field            | Detail                                                                                                                                                                                                                                                                                                                 |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Part of**      | `core-component-00/platform/maintenance-records/2026-08-13-mcp-server-powershell-cross-platform/maintenance-record.md`, pipeline stage 1 — Investigation (`core-component-00/platform/maintenance-records/pipeline.md`)                                                                                                |
| **Trigger**      | CEO flagged that this session's actual operating environment is Windows WSL Ubuntu, not a plain native Linux/macOS machine as this topic's records (Items #8–#10) have been describing it, and asked whether this session's maintenance changes are correctly supported and adapted for that environment specifically. |
| **State before** | Items #8, #9, #10 all closed and verified on this machine, but none of their verification steps had explicitly checked for WSL-specific behavior (drvfs vs. native filesystem, `sys.platform` reporting under WSL, symlink semantics) as distinct from generic Linux.                                                  |

**Actions taken:**

1. Confirmed the kernel and filesystem this session actually runs on: `uname -a` reports `6.18.33.2-microsoft-standard-WSL2`; `df -T .` and `mount` show the repository sits on `/dev/sdd` (`ext4`, native), not a Windows-mounted `/mnt/c` drvfs path.
2. Checked how Python's own platform APIs report this environment: `sys.platform` returns `"linux"`, `platform.system()` returns `"Linux"` — indistinguishable from bare-metal Linux at the Python level.
3. Read `.claude/hooks/mcp-config-platform-check.py` (Item #8's pre-existing self-heal hook) and `.claude/hooks/mcp-venv-bootstrap.py` (Item #8's new hook) in full to check whether either branches on `sys.platform`/`win32` in a way that could misclassify WSL.
4. Read the live root `.mcp.json` to confirm which interpreter suffix is currently selected for both servers.
5. Spot-checked one of Item #9's 8 repaired `AGENTS.md` symlinks (`core-component-00/framework/04-retrieval-augmented-generation/AGENTS.md`) directly on disk — `ls -la` and `os.readlink()` — to confirm the fix produced a genuine POSIX symlink on this filesystem, not something drvfs-specific.
6. Confirmed `uv` (used by `mcp-venv-bootstrap.py` for Item #8, and by both servers' own dependency management) resolves on `PATH` to a real Linux binary in this environment.

**Verification:**

| Check performed                                              | Result                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `uname -a` / `/proc/version`                                 | Confirmed genuine WSL2: `microsoft-standard-WSL2` kernel, not native Linux, not WSL1                                                                                                                                                                                                          |
| `df -T .` / `mount`                                          | Repository is on `/dev/sdd`, `ext4`, native WSL filesystem (`/home/ubuntu/...`) — **not** under `/mnt/c` (Windows drvfs), which would have weaker symlink and performance characteristics                                                                                                     |
| `sys.platform` / `platform.system()`                         | `"linux"` / `"Linux"` — WSL2 is indistinguishable from bare-metal Linux at this level, because it runs a real Linux kernel                                                                                                                                                                    |
| `mcp-config-platform-check.py`'s branching logic             | Does **not** branch on `sys.platform` at all — it resolves each candidate interpreter path with `Path(...).is_file()` and picks whichever one actually exists on disk. This makes it correct for WSL by construction, not by explicit WSL-awareness: it never needed to know what OS it's on. |
| `mcp-venv-bootstrap.py`'s branching logic                    | Same pattern — reads the sibling hook's file-existence-based log entry and shells out to `uv sync`, a genuinely cross-platform tool. No `sys.platform`/`win32` branching present.                                                                                                             |
| Live `.mcp.json`                                             | Both servers' `"command"` correctly use the `.venv/bin/python` (POSIX) suffix — matches this machine's actual venv layout                                                                                                                                                                     |
| `ls -la` / `os.readlink()` on a repaired `AGENTS.md` symlink | `lrwxrwxrwx`, target `'CLAUDE.md'` (9 bytes, no trailing newline) — a genuine, correctly-formed POSIX symlink, identical in kind to every other correct `AGENTS.md` symlink in the repo; native ext4 gives WSL2 full symlink support, unlike drvfs                                            |
| `which uv` / `uv --version`                                  | `/home/ubuntu/.local/bin/uv`, `uv 0.12.0 (x86_64-unknown-linux-gnu)` — resolves correctly on `PATH` in this environment, consistent with Item #8's `log/20` live test of `mcp-venv-bootstrap.py`'s `uv sync` call already having succeeded on this exact machine                              |

| Field                     | Detail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Outcome**               | No WSL-specific gap found in any of this session's Item #8/#9/#10 changes. All three are correct for this environment, but for two different reasons: (a) both `SessionStart` hooks are OS-detection-agnostic by design (they check which interpreter file actually exists rather than branching on a platform string), so WSL never needed special-casing in the first place; (b) the repository happens to sit on WSL2's native ext4 filesystem rather than a Windows-mounted drvfs path, so the symlink repair (Item #9) and the FAISS-directory fix (Item #10, plain `pathlib.mkdir`) get full native POSIX semantics with none of drvfs's known symlink/permission caveats. Had this repo instead lived under `/mnt/c/...`, the symlink repair specifically would need re-verification — drvfs's symlink support is more limited — but that is not this machine's configuration. |
| **Handoff to next stage** | No new Open Follow-Up Item opened — this is a clean verification pass, not a defect. Worth noting for awareness only (not acted on without separate Approval, and not a defect in this session's changes): `.claude/rules/git-workflow.md`'s "Repository Information" section still states a Windows-native path (`c:\Users\ASUS\Documents\Code\Local\agent-global-base`) as the repository root, which does not match this session's actual WSL path — a pre-existing documentation staleness unrelated to Items #8–#10, out of this investigation's scope.                                                                                                                                                                                                                                                                                                                          |
