# requirements.txt — core-component-00/examples/langchain-ecosystem-reference
#
# Deliberately UNPINNED for this initial install so the resolver picks current
# releases; the exact versions actually resolved are then recorded in
# requirements.lock.txt (generated by `pip freeze`, checked in) and restated
# in README.md. Re-pin this file to that lock once the project stabilises —
# see 00-conventions-and-baseline.md §3 for why exact pinning is a SECURITY
# control here, not hygiene (the LangGraph checkpointer CVE chain).
#
# SECURITY FLOORS — do not resolve below these:
#   langgraph                    >= 1.0.10
#   langgraph-checkpoint-sqlite  >= 3.0.1
#   langgraph-checkpoint-redis   >= 1.0.2

langchain>=1.0
langgraph>=1.0.10
langgraph-checkpoint-sqlite>=3.0.1
langchain-anthropic>=0.3
langchain-openai>=0.3
langchain-mcp-adapters>=0.1
deepagents>=0.1

pytest>=8.0
pytest-asyncio>=0.24
