[project]
name = "cc00-context-engineering"
version = "0.1.0"
description = "CC-00 Layer 2 (Context Engineering) — test environment for implementations/"
readme = "README.md"
requires-python = ">=3.10"

# implementations/ itself has no third-party dependencies (stdlib only). pytest is
# needed to run testing/.
dependencies = ["pytest>=8.0.0"]

[tool.uv]
package = false
