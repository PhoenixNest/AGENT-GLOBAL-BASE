# RAG System Dependencies
#
# Currently required by implementations/, testing/, and tools/: none beyond pytest, which
# is declared in this module's own pyproject.toml (`uv sync` from this directory) — not
# here. All four implementation files (chunker.py, retrieval.py, pipeline.py,
# pii_masking.py), the full testing/ suite, and both tools/ scripts (initialize.py,
# thermal_guardian.py) import stdlib only.
#
# Everything below is aspirational: dependencies for the broader "enterprise-grade RAG"
# architecture this module's fundamentals/ and patterns/ document, but that
# implementations/ does not yet build and testing/ does not yet exercise. None of it is
# imported anywhere in this module today. Install only when actually implementing one of
# these pieces — not as part of a general environment setup, and not to run the existing
# test suite (see this module's CLAUDE.md § Running the Test Suite).

# Core dependencies
python-multipart>=0.0.6
aiofiles>=23.2.1
pydantic>=2.5.0

# Embedding models
sentence-transformers>=2.3.0
torch>=2.1.0
transformers>=4.35.0

# Vector database client
qdrant-client>=1.7.0
weaviate-client>=3.25.0

# Redis for caching and ACL
redis>=5.0.1

# API framework
fastapi>=0.104.0
uvicorn>=0.24.0

# HTTP client
requests>=2.31.0
httpx>=0.25.0

# Text processing and chunking
langchain>=0.1.0
langchain-community>=0.0.19
spacy>=3.7.0
# Note: spaCy language models are NOT installed via pip.
# Run: python -m spacy download en_core_web_sm

# Token counting
tiktoken>=0.5.0

# PDF processing
PyMuPDF>=1.23.0
pdfplumber>=0.10.3

# HTML to Markdown conversion
markdownify>=0.4.1

# Database connectivity
sqlalchemy>=2.0.0

# Evaluation libraries
datasets>=2.14.0
sacrebleu>=2.3.0
bert-score>=0.3.5

# Visualization (for monitoring dashboards)
matplotlib>=3.8.0
seaborn>=0.12.2

# Dev tooling for whichever of the above gets implemented (not needed for the existing
# lightweight suite -- that's covered by this module's own pyproject.toml)
pylint>=2.17.0
black>=23.11.0
# Note: prettier is a Node.js formatter — install via: npm install -g prettier
mypy>=1.5.0
ruff>=0.0.287

# Security and audit
# Note: hashlib is a Python standard-library module; no pip install required.
cryptography>=41.0.0

# Logging
structlog>=23.2.0

# Monitoring
prometheus-client>=0.18.0

# Optional: Cohere reranking
cohere>=4.35

# Optional: Elasticsearch for hybrid search
elasticsearch>=8.9.0
